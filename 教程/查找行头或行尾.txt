indstr 语法中，^表示行首，$表示行尾
举个例子：
findstr "^am"   1.txt     表示在文件1.txt中查找以am开头的字符串
findstr "am$"   1.txt     表示在文件1.txt中查找以am结尾的字符串

所以要查找以$开头的字符串，必须在$前面加一个转义符 \ ,去掉$的特殊含义，
让系统把它当普通字符看待

我是忘了dos下的转义符 \ 
因为批处理语法中的转义符刚好是 ^
